# migrationScripts
scripts for devops migrations
These scripts can be used to migrate / back up / restore DevOps processes.
The backup and restore scripts can also be used to migrate kubernetes clusters using velero.
With the gitlab issues script you can migrate issues 1 on 1 from gitlab to azure devops.
